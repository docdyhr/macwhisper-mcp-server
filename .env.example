# Copy to .env and adjust. Loaded automatically by direnv if you use it.

# Colon-separated list of directories the server is allowed to read from.
# Paths outside these directories are rejected.
MACWHISPER_ALLOWED_PATHS=~/Desktop:~/Downloads:~/Transcriptions

# Path to the MacWhisper CLI. If MacWhisper.app is installed in /Applications,
# the server auto-detects /Applications/MacWhisper.app/Contents/MacOS/mw. Override
# here if you installed it elsewhere or symlinked `mw` onto your PATH.
MACWHISPER_CLI=/Applications/MacWhisper.app/Contents/MacOS/mw

# Log file path. The server never writes logs to stdout (reserved for MCP protocol).
MACWHISPER_LOG_PATH=~/Library/Logs/macwhisper-mcp.log

# Optional per-directory default language (ISO 639-1 code, or "auto"), passed as
# --language when a file's directory matches. Colon-separated dir=lang pairs.
# An explicit `language` argument to transcribe_audio always overrides this.
# MACWHISPER_LANGUAGE_DEFAULTS=~/Desktop/DK=da:~/Desktop/DE=de

# Optional: path to the whisper-cli binary (whisper.cpp), if not on PATH.
# Only used when transcribe_audio is called with engine="whisper-cpp".
# Install with: brew install whisper-cpp
# MACWHISPER_WHISPERCPP_BINARY=whisper-cli

# Optional: directory containing your GGML .bin model files, required to use
# engine="whisper-cpp" at all. Download models yourself (this server never
# downloads anything) from https://huggingface.co/ggerganov/whisper.cpp
# MACWHISPER_WHISPERCPP_MODEL_DIR=~/whisper-models

# Disable FastMCP's PyPI update check — this server is privacy-first, no outbound calls.
FASTMCP_CHECK_FOR_UPDATES=off
